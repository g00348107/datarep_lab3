import React from "react";

export class Footer extends React.Component{//The export keyword allows this class to be exported to the App class
  
    render(){
        return(
            <div>
                <h1>This is the footer component.</h1>
            </div>

        );
    }
}