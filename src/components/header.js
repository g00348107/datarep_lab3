import React from "react";

export class Header extends React.Component{//The export keyword allows this class to be exported to the App class
  
    render(){
        return(
            <div>
                <h1>This is the header component.</h1>
            </div>

        );
    }
}
